pub const THRESHOLD: f64 = 0.07;
pub const DEFAULT_WIDTH: usize = 1024;
pub const DEFAULT_HEIGHT: usize = 1024;
pub const MAX_ELEVATION: u8 = 255;
pub const MAX_SLOPE: f32 = 1.0;

pub enum Dir {
    N,
    NE,
    E,
    SE,
    S,
    SW,
    W,
    NW    
}
